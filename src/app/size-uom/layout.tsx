import { ReactNode } from 'react';

export const metadata = {
  title: 'Size UOM | eCom Furniture Admin',
  description: 'Manage your size UOM.',
};

export default function SizeUomLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
