import { ReactNode } from 'react';

export const metadata = {
  title: 'Locations | eCom Furniture Admin',
  description: 'Manage your locations.',
};

export default function LocationsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
