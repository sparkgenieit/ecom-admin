import { ReactNode } from 'react';

export const metadata = {
  title: 'Assembly Types | eCom Furniture Admin',
  description: 'Manage your assembly types.',
};

export default function AssemblyTypesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
