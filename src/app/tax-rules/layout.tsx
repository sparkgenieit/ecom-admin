import { ReactNode } from 'react';

export const metadata = {
  title: 'Tax Rules | eCom Furniture Admin',
  description: 'Manage your tax rules.',
};

export default function TaxRulesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
