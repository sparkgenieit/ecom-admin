import { ReactNode } from 'react';

export const metadata = {
  title: 'Warranties | eCom Furniture Admin',
  description: 'Manage your warranties.',
};

export default function WarrantyLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
