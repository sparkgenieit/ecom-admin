import { ReactNode } from 'react';

export const metadata = {
  title: 'Shipping Partners | eCom Furniture Admin',
  description: 'Manage your shipping-partners.',
};

export default function ShippingPartnerLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
