import { ReactNode } from 'react';

export const metadata = {
  title: 'Room Types | eCom Furniture Admin',
  description: 'Manage your room types.',
};

export default function RoomTypesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
