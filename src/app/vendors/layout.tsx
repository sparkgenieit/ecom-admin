import { ReactNode } from 'react';

export const metadata = {
  title: 'Vendors | eCom Furniture Admin',
  description: 'Manage your vendors.',
};

export default function VendorsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
