import { ReactNode } from 'react';

export const metadata = {
  title: 'Styles | eCom Furniture Admin',
  description: 'Manage your styles.',
};

export default function StyleLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
